<div class="container-py-5">
  <div class="row">

    <div class="col-md-3">
      <nav class="navbar">
        <p>ジャンル検索</p>

      </nav>
    </div>

    <div class="col-md-４">
      <%= image_tag @item.get_item_image(220,220) %>
    </div>

    <div class="col-md-5">
      <div>商品名<%= @item.name %></div>

      <div>商品説明<%= @item.introduction %></div>
      <div>￥<%= @item.add_tax_price.to_s(:dalimited) %>（税込）</div>

        <div>
          <%= form_with model: @item, url: cart_items_path, local:true do |f| %>
            <%= f.select :id, *[1..10] %>
            <%= f.hidden_field :id, :value => @item.id %>
            <%= f.submit "カートに入れる", class: "btn btn-sm btn-secondary" %>
          <% end %>

        </div>
    </div>
  </div>
</div>